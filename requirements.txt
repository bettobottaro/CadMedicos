# CadMedicos - Requisitos para Python 3.8
# Compatível com Windows 7 x86

# Bibliotecas padrão (já incluídas no Python):
# tkinter, csv, os, dataclasses, typing, datetime

# Bibliotecas de terceiros necessárias:
xlrd==1.2.0          # Para leitura de arquivos .xls (versão compatível com Python 3.8)
openpyxl==3.0.10     # Para leitura de arquivos .xlsx (versão compatível com Python 3.8)

# Nota: xlrd 2.0+ não suporta mais .xls, por isso a versão 1.2.0
# Para instalar: pip install -r requirements.txt
# OAuth2/XOAUTH2 para Gmail via SMTP STARTTLS
google-auth==2.22.0
google-auth-oauthlib==1.0.0
